# Claude adapter

Read and follow [AGENTS.md](AGENTS.md), including its linked provenance and
workflow document. `AGENTS.md` is the canonical project instruction source; this
file keeps Claude Code compatibility without creating a second rule set.
