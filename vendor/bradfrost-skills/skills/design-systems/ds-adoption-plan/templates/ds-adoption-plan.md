# Phase 4 — Build schedule: <Product> → <Design System> adoption

_Written <date> from `MAPPING.md` · re-run the teardown after each wave and append the delta below_

**End state:** <the measurable finish line — e.g. custom CSS from <n> → under <n> lines (sanctioned reset + documented Keeps); zero orphaned hooks; every remaining custom line carries a Keep rationale or upstream issue link.>

## Wave 1 — <name> (Effort: <roll-up>)

- [ ] <mapping row ID> — <one-line action>
- [ ] File upstream: <the System gap / Recipe candidate issues, filed now so later waves aren't blocked>

**Done when:** <observable conditions> · **Verify:** <what gets run>

## Wave 2 — <name> (Effort: <roll-up>)

- [ ] …

**Done when:** … · **Verify:** …

<…further waves; each is a mergeable PR or small stack…>

## Wave N — Gap-blocked & upstream-paced

| Item | Blocked on | Local action when unblocked |
|---|---|---|
| <mapping row> | <issue link> | <the swap/compose to perform> |

## Cadence & verification

- After each wave: <build + verify scripts + scanner re-run>; append the score/surface delta here
- Upstream watch: check the gap-blocked table's issues each re-run
- Telemetry: <how the system will see this product's adoption, if it has adoption/activity tracking>

## Re-run deltas

_<date>: <patterns retired, lines removed, hooks closed, gaps unblocked — appended by Mode 3 re-runs>_
